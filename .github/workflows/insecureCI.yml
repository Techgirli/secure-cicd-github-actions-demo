name: Insecure CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Print secret (BAD PRACTICE)
        run: echo "API_KEY=hardcoded-secret-123"
